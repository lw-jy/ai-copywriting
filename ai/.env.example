# ─── AI 文案生成 API ────────────────────────────────────────────────
#
# 留空 = Mock 模式（无需配置，直接运行即可看到 UI 效果）
# 填写 = AI 模式（当前使用 DeepSeek 生成真实文案）
#
# DeepSeek API Key 获取：https://platform.deepseek.com/api_keys
#
# OPENAI_API_KEY 这个变量名是历史遗留，值填 DeepSeek 的 key 即可
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MongoDB 连接串（可选，不配则收藏功能不可用）
# 从 MongoDB Atlas 获取：https://cloud.mongodb.com
# MONGODB_URI=mongodb+srv://用户名:密码@cluster.xxxxx.mongodb.net/copywriting
